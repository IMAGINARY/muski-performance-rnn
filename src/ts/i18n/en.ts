// strings is an object with string values. The keys are the same in both files.
const strings : { [key: string]: string } = {
  loudness: 'loudness',
  numberOfNotes: 'number of notes',
};

export default strings;
